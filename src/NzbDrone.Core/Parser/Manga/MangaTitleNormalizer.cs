using System;

namespace NzbDrone.Core.Parser.Manga
{
    // Single source of truth for manga title canonicalization (D-05).
    // Wave 0 stub — algorithm lands in Plan 02-04.
    public static class MangaTitleNormalizer
    {
        public static string Normalize(string title)
        {
            throw new NotImplementedException(
                "MangaTitleNormalizer is a Wave 0 stub. Real implementation lands in Plan 02-04.");
        }
    }
}
