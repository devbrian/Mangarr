// Sonarr divergence: Phase 15 Plan 15-12 — STUB pass-through.
import React from 'react';
type Props = { children: React.ReactNode };
export default function EpisodeFileProvider({ children }: Props) {
  return <>{children}</>;
}
