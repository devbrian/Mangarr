// Phase 7 Plan 07-05 — Manga sibling per D-07; RESEARCH Lock #14 governs the
// literal set. Existing TV variants preserved verbatim.
//
// Phase 8 cleanup: collapse 'episode' / 'season' when Tv/ deletes; the file
// becomes manga-only.

type InteractiveSearchType = 'episode' | 'season' | 'chapter' | 'manga';

export default InteractiveSearchType;
