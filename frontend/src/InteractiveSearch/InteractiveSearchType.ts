// Manga-shape literal set. The TV-shape 'episode' / 'season' literals were
// retired with the OverrideMatch TV fallback (issue #263).
type InteractiveSearchType = 'chapter' | 'manga';

export default InteractiveSearchType;
