# DIVERGENCE.md

Mangarr is a fork of Sonarr v5. This document enumerates every intentional divergence from `Sonarr/Sonarr` `v5-develop`, with rationale.

Format: flat table grouped by subsystem. Updated by hand when divergences land or move from "planned" to "merged." This is a **static inventory** — the upstream-sync cadence schedule and the CI divergence-flag job are deferred per CONTEXT.md D-17 and may be reopened in a future milestone. See [.planning/phases/00-pre-code-decisions/00-CONTEXT.md](./.planning/phases/00-pre-code-decisions/00-CONTEXT.md) `<deferred>` section.

Type taxonomy: `extend`, `replace`, `delete`, `new`, `preserve`.

## Preserved Unchanged (Sonarr Wins We Keep)

| Subsystem | Reason We Keep It |
|-----------|-------------------|
| `src/NzbDrone.Core/ThingiProvider/` | Auto-discovery plugin pattern. Manga sources, download clients, metadata sources, notifications, specifications, and health checks slot in unchanged via reflection-based assembly scanning. |
| `src/NzbDrone.Core/DecisionEngine/Specifications/` (pattern, not contents) | `IDownloadDecisionEngineSpecification` shape preserved; specific TV-shaped specs swap in Phase 5 for manga equivalents. |
| Dapper + FluentMigrator | Phase 1 ships fresh manga baseline migration; ORM + migration pattern unchanged. |
| `src/NzbDrone.SignalR/` | Real-time push to UI; reused as-is for Queue, History, Manga, and Chapter updates. |

## Already-Merged Divergences (on `Mangarr-v0`)

| File / Path | Type | Phase | Rationale |
|-------------|------|-------|-----------|
| `src/NzbDrone.Core/Tv/Series.cs` (lines 20-21, 29-30) | extend | pre-Phase-1 | Added `MalIds: HashSet<int>` and `AniListIds: HashSet<int>` to support manga cross-reference; collection type is `HashSet<int>` (not `List<int>` as some prior `.planning/codebase/` docs suggest). Kept on `Series` (not new `Manga` type) to defer rename to Phase 8 per leaf-first/rename-last sequence. |

## Planned Divergences (Not Yet on `Mangarr-v0`)

| File / Path | Type | Phase | Rationale |
|-------------|------|-------|-----------|
| `src/NzbDrone.Core/Datastore/Migration/000-…223-…` | delete | Phase 1 | Drop 224 inherited TV-shaped migrations; replace with manga baseline per [fresh-schema-confirmed.md](./.planning/decisions/fresh-schema-confirmed.md). |
| `src/NzbDrone.Core/Datastore/Migration/M001_baseline.cs` | new | Phase 1 | New manga baseline migration with composite indexes pre-built (per fresh-schema baseline floor). |
| `src/NzbDrone.Core/Datastore/ConnectionStringFactory.cs` (BusyTimeout 1000→5000) | replace | Phase 1 | Per D-15 baseline floor; reduces SQLITE_BUSY surface area under v1 concurrency profile. |
| `src/NzbDrone.Core/MetadataSource/SkyHook/` | replace | Phase 2 | Replace TVDB/SkyHook with `IMetadataSource` plugin contract serving MangaDex / AniList / MAL. |
| `src/NzbDrone.Core/Parser/Parser.cs` (TV regex) | replace | Phase 2 | Replace TV-show regex with manga release-name regex (decimal chapter numbers, scanlation groups, `Ch.45-47` ranges). |
| `src/NzbDrone.Core/Indexers/Newznab/`, `Torznab/`, `Nyaa/`, `BroadcastheNet/`, `HDBits/`, etc. | delete | Phase 3 | TV/anime indexers; Mangarr's source model is aggregator websites, not Newznab/Torznab. |
| `src/NzbDrone.Core/Indexers/Mangadex/` (new) | new | Phase 3 | MangaDex bedrock indexer; Apache-2.0 attribution to `keiyoushi/extensions-source` if Mihon-derived per [source-onboarding-methodology.md](./.planning/decisions/source-onboarding-methodology.md). |
| `src/NzbDrone.Core/Indexers/Comix/` (new) | new | Phase 3 | comix.to reference port #1 per onboarding pipeline. |
| `src/NzbDrone.Core/Indexers/MangaFire/` (new) | new | Phase 3 | MangaFire reference port #2 per onboarding pipeline. |
| `src/NzbDrone.Core/Download/Clients/InProcess/` (new) | new | Phase 4 | Novel `InProcessImageDownloadClient` — no peer-fork precedent; bounded `Channel<T>` with resumable state. |
| `src/NzbDrone.Core/MediaFiles/Archive/` (new) | new | Phase 4 | `IChapterArchiver` strategy: CBZ + folder-of-images + ComicInfo.xml v2.0 / v2.1 dual-write. |
| `src/NzbDrone.Core/Qualities/Quality.cs` | delete | Phase 5 | TV resolutions enum; manga has no quality concept (replaced by Custom Formats + TranslationProfile per PROJECT.md Key Decision). |
| `src/NzbDrone.Core/Profiles/Translation/` (new — `TranslationProfile.cs`, `TranslationProfileService.cs`, `TranslationProfileRepository.cs`) | new | Phase 5 | Ordinal language-preference entity. Phase 5 scope expansion per [cf-only-walkthrough.md](./.planning/decisions/cf-only-walkthrough.md) verdict signoff (2026-05-01: TranslationProfile added). Applied as ordinal gate *before* Custom Format total-score in Decision Engine. |
| `src/NzbDrone.Core/Datastore/Migration/M002_translation_profile.cs` | new | Phase 5 | FluentMigrator migration for `TranslationProfile` entity + per-Manga FK. Required for TPROFILE-04 persistence. |
| `src/NzbDrone.Core/DecisionEngine/Specifications/` (TV-specific specs: `MonitoredEpisodeSpecification`, `MultiEpisodeSpecification`, `AnimeVersionUpgradeSpecification`, `RepackSpecification`, etc.) | replace | Phase 5 | TV-shaped specs replaced with manga equivalents; `IDownloadDecisionEngineSpecification` shape preserved. |
| `src/NzbDrone.Core/CustomFormats/` (architecture, not contents) | preserve | Phase 5 | CF infrastructure preserved; `LanguageSpecification` reusable as-is per the existing Sonarr pattern. |
| `src/Sonarr.Api.V5/` (Series/Episode controllers) | extend | Phase 7 | Add `/api/v5/manga`, `/chapter`, `/library` controllers alongside legacy Series/Episode controllers; rename in Phase 8. |
| `frontend/src/Series/`, `frontend/src/Episode/`, `frontend/src/AddSeries/` | extend | Phase 7 | Add parallel `Manga/`, `Chapter/`, `AddManga/` directories; rename and delete old in Phase 8. |
| All `Series → Manga`, `Episode → Chapter` C# rename | replace | Phase 8 | Domain rename deferred to Phase 8 cutover per leaf-first/rename-last; preserves upstream-merge ability through Phase 7. |
| `src/Sonarr.Api.V3/` (44 controllers) | (decision pending) | Phase 8 | Per PITFALLS.md #5 prevention strategy 5: V3 stays or is deleted in a single named commit. CONTEXT.md does not lock this; flagged here so the decision is not forgotten. |
| `THIRD-PARTY-NOTICES.md` | new | Phase 3 | Apache-2.0 attribution register; first entry = `keiyoushi/extensions-source` if any port draws from it. Mechanism committed in [source-onboarding-methodology.md](./.planning/decisions/source-onboarding-methodology.md). |
| `README.md` (rebrand + 3 dormant prior-art repos acknowledgement per BRAND-03) | replace | Phase 8 | Footer-style acknowledgement of `donderjoekel/Mangarr` (archived 2025-04-30), `hyminix/Mangarr`, `tnrd-org/Mangarr`. |
| `src/NzbDrone.Core/Indexers/Http/HttpAggregatorBase.cs` | extend | Phase 1 | New abstract base class extending `HttpIndexerBase<TSettings>` to inject per-`SourceKey` rate budget + honest-by-default User-Agent. Phase 3 source plugins (MangaDex/comix.to/MangaFire) extend this. Per D-10/D-11/D-12/D-13/D-14. |
| `src/NzbDrone.Core/Indexers/DownloadProtocol.cs` | extend | Phase 1 | New enum value `Http = 3` added (was `Unknown=0, Usenet=1, Torrent=2`). Required for in-process downloader (Phase 4). No `switch` statements use this enum exhaustively (verified via grep) — additive only. |
| `src/NzbDrone.Core/Datastore/Migration/*.cs` (224 files) → `001_mangarr_baseline.cs` | replace | Phase 1 | Single fresh-baseline migration replaces Sonarr's 224 inherited TV-shaped migrations (per D-14 Phase 0 + D-01 Phase 1 floor-only). Recreates Tv tables verbatim per D-02 for `Tv/` C# compile-compat (sit empty at runtime; deleted in Phase 8). Adds new manga tables (Manga, Chapters), nullable manga columns to History/Blocklist, and the five locked composite indexes from D-15. |
| `src/NzbDrone.Core/Datastore/BasicRepository.cs` | extend | Phase 1 | Polly `RetryStrategy.Execute(...)` wrapping extended from Insert/Update only (lines 214/414/426) to Find / Get / Get(IEnumerable) / Delete / DeleteMany / Upsert / Purge / Count / GetPagedRecordCount paths. Same `MaxRetryAttempts = 3` policy on `SQLITE_BUSY`. Per D-15 Phase 0 baseline floor. |
| `src/NzbDrone.Core/Datastore/ConnectionStringFactory.cs` | extend | Phase 1 | SQLite `BusyTimeout` increased from `1000` to `5000` ms per D-15 Phase 0 baseline floor / SQLite docs recommendation for concurrent-write workloads. |
| `src/NzbDrone.Core/Indexers/IIndexer.cs` + `IndexerBase.cs` + `HttpIndexerBase.cs` + `IIndexerRequestGenerator.cs` | extend | Phase 3 | Adds 2 manga `Fetch` overloads + matching `IIndexerRequestGenerator.GetSearchRequests` overloads alongside the existing 7 TV ones (D-01). TV indexers (Newznab, Nyaa, Torznab, BroadcastheNet, Fanzub, FileList, HDBits, IPTorrents, Torrentleech, TorrentRss) gain 2 trivial no-op overloads each (compile-error-driven fan-out — no behavior change). Phase 8 deletes the TV overloads with `Tv/`. |
| `src/NzbDrone.Core/Indexers/Http/HttpAggregatorBase.cs` | extend | Phase 3 | Adds manga `Fetch(MangaSearchCriteria)` + `Fetch(ChapterSearchCriteria)` ABSTRACTS (D-02) + `GetDownloadHeaders(ReleaseInfo)` virtual hook (D-14 — Phase 4 in-process downloader consumes per-source `Referer`/`Origin` headers). |
| `src/NzbDrone.Core/IndexerSearch/Definitions/MangaSearchCriteriaBase.cs` + `MangaSearchCriteria.cs` + `ChapterSearchCriteria.cs` | new | Phase 3 | Parallel hierarchy to TV `SearchCriteriaBase` (D-06; clean leaf-first separation). Phase 8 collapses both bases when `Tv/` deletes. ChapterNumber is `decimal` per Phase 2 D-12 widen. `PreferredLanguages` is advisory only (D-07; Phase 5 TranslationProfile authoritative). |
| `src/NzbDrone.Core/Indexers/IndexerSourceStatus*` (model + repository + service + ChangedEvent) + `001_mangarr_baseline.cs` IndexerSourceStatus table + `TableMapping.cs` registration | new | Phase 3 | Per-`SourceKey` escalation for HttpAggregatorBase descendants (D-17 Q-2 Option A — own ModelBase subclass + sibling table for Phase 8 cleanup symmetry). Two indexer instances with same SourceKey share disable state. TV indexers stay per-`ProviderId`. Sonarr 10-level `EscalationBackOff.Periods` cadence preserved (D-18). |
| `src/NzbDrone.Core/Parser/Model/ReleaseInfo.cs` | extend | Phase 3 | Adds `ScanlationGroup` + `TranslatedLanguage` string fields (Q-4 / SOURCE-04). Indexer-supplied wins per Phase 2 D-10; parser fallback applies only when indexer omits. Phase 5 Custom Formats consume the structured fields for group-match + language-match rules. Phase 8 may rename / collapse fields with manga rename, but these survive. |
| `src/NzbDrone.Core/Indexers/MangaDex/`, `src/NzbDrone.Core/Indexers/Comix/`, `THIRD-PARTY-NOTICES.md` | new | Phase 3 | v1 source slate: MangaDex (BEDROCK; direct port from MangaDex API docs) + comix.to (reference port #1 from keiyoushi/extensions-source PR #11658). MangaFire DESCOPED to v2 (D-19; resolved 2026-05-02 from RESEARCH.md Q-1) — keiyoushi MangaFire is HYBRID HTML+WebView; conflicts with D-10 + D-13. v2 reopens when SOLVE-01 ships. THIRD-PARTY-NOTICES.md captures Apache-2.0 attribution per source-onboarding-methodology.md §4. |
| `src/NzbDrone.Core/MediaFiles/ChapterArchiving/Metadata/IMetadataWriter.cs` | new | Phase 4 | Mangarr `IMetadataWriter` plugin contract is a divergence from Sonarr's `Extras/` per-provider metadata sidecar pattern. Sonarr writes per-`MetadataDefinition` provider files via `MetadataService` + `IMetadataDefinition`; Mangarr's chapter-archive metadata writes go through a sibling `IMetadataWriter` contract specific to chapter archives. v1 ships ONE writer (`ComicInfoMetadataWriter`); user toggles via global `Config.MetadataFormats`. Phase 8 may collapse the two patterns if convergence is feasible; v1 keeps them separate. Per Phase 4 D-14 (CONTEXT.md). |
| `src/NzbDrone.Core/Download/Clients/InProcess/InProcessImageDownloadClient.cs` | new | Phase 4 | `InProcessImageDownloadClient` extends `DownloadClientBase<TSettings>` directly — divergence from Sonarr's `UsenetClientBase` / `TorrentClientBase` middle-tier convention. Manga is neither Usenet nor Torrent — Phase 1 D-04 introduced `DownloadProtocol.Http` as the third enum value. The closest behavioral analog is `UsenetBlackhole` (filesystem-as-truth `GetItems()`); Phase 4 swaps the watch-folder for the `ChapterDownloadState` table so polling is O(rows). Phase 8 may add an `HttpClientBase` middle-tier when `Tv/` collapses. Per Phase 4 D-04 + plan 04-03. |

## Phase 5 — Decision Engine + TranslationProfile + Custom Formats + Naming (2026-05-03)

Sibling-divergence entries per Phase 5 plan 05-07 (Wave 4 governance task). Every NEW manga-side
sibling file gets a row; the parallel hierarchy collapses with `Tv/` deletion in Phase 8.

| File / Path | Type | Phase | Rationale |
|-------------|------|-------|-----------|
| `src/NzbDrone.Core/Profiles/Translations/` (entity + repo + service + InUseException + UpdatedEvent) | new | Phase 5 | Phase 0 cf-only-walkthrough.md verdict signed off 2026-05-01: TranslationProfile added as outer ordinal language gate. NO TV analog (TV uses Sonarr's `Language` enum embedded in `QualityProfile`). Per Phase 5 D-01..D-04. Phase 8 cleanup: collapse to canonical `Profiles/` namespace. |
| `src/NzbDrone.Core/Profiles/CustomFormats/` (entity + repo + service + InUseException + UpdatedEvent) | new | Phase 5 | Per Phase 5 D-07: CF score thresholds live on a separate entity from TranslationProfile (orthogonal user concerns: language preference vs CF scoring). NULLABLE `MaxFormatScore` per Adaptation Hotspot 4. NO TV analog (TV embeds CF scoring in `QualityProfile`). Phase 8 cleanup: collapse to canonical `Profiles/` namespace. |
| `src/NzbDrone.Core/DecisionEngine/Manga/` (interface + maker + comparer + DTO) | new | Phase 5 | Per Phase 5 D-05: parallel manga decision-maker per "Compile-error-driven additive contract" pattern (Phase 3 LEARNINGS verbatim). `IMangaDecisionEngineSpecification` takes `RemoteChapter` not `RemoteEpisode` — TV specs cannot accidentally fire on a manga release (Pitfall 6 mitigation). Sibling to `src/NzbDrone.Core/DecisionEngine/{IDownloadDecisionEngineSpecification.cs, DownloadDecisionMaker.cs, DownloadDecisionComparer.cs}`. Phase 8 collapse: rename interfaces; drop the discriminator. |
| `src/NzbDrone.Core/DecisionEngine/Manga/MangaDownloadDecisionComparer.cs` (D-08 ordering) | new | Phase 5 | Per Phase 5 D-08: ordering Language rank → CF score → IndexerPriority → Age → Size. Manga has NO Quality/EpisodeCount/EpisodeNumber/Peers/Protocol concepts (TV ordering at `DownloadDecisionComparer.cs:30-41`). IndexerPriority promoted above Age/Size per user direction (source-stability matters more than freshness at human-library scale). Phase 8 collapse: canonical comparer. |
| `src/NzbDrone.Core/DecisionEngine/Manga/Specifications/` (11 specs) | new | Phase 5 | Per Phase 5 D-06: 5 core gates (MonitoredManga, MonitoredChapter, ChapterRequested, AlreadyImportedChapter, Blocklist [Phase 5 stub — wired in Phase 6]) + LanguageInTranslationProfile (TPROFILE outer enforcer) + CustomFormatMinimumScore (CF inner enforcer) + 4 operational gates (MinimumAge, AcceptableSize, MaximumSize, QueueDuplicate [Phase 5 stub]). All implement `IMangaDecisionEngineSpecification` only (Pitfall 6 single-interface guard). Phase 8 collapse: into canonical `DecisionEngine/Specifications/`. |
| `src/NzbDrone.Core/CustomFormats/Specifications/Manga/` (4 NEW manga CF specs) | new | Phase 5 | Per Phase 5 D-09: TranslatedLanguageSpecification (BCP-47 string — distinct from Sonarr's TV-region `LanguageSpecification`), ScanlationGroupSpecification (regex on `ReleaseInfo.ScanlationGroup` — distinct from Sonarr's `ReleaseGroupSpecification`), SourceKeySpecification (Phase 3 D-17 indexer key), ChapterTypeSpecification (Phase 2 D-09 enum). Implement `ICustomFormatSpecification`; auto-discovered via DryIoc. Phase 8 collapse: into canonical `CustomFormats/Specifications/`. |
| `src/NzbDrone.Core/CustomFormats/MediaType.cs` + `ICustomFormatSpecification.AppliesTo` discriminator | new | Phase 5 | Per Phase 5 D-10: hide TV CF specs (`Resolution`, `Source`, `ReleaseType`) from manga UI; manga CF specs return `MediaType.Manga`; reusable specs (`ReleaseGroup`, `ReleaseTitle`, `IndexerFlag`, `Size`) return `MediaType.All`. Phase 7 React filters CF authoring UI by AppliesTo. Phase 8 cleanup: drop both — only manga remains after Tv/ deletes. |
| `src/NzbDrone.Core/CustomFormats/MangaCustomFormatInput.cs` (sibling to CustomFormatInput) | new | Phase 5 | Per Phase 5 D-09 + Open Question 1 + Pitfall 4: derives from `CustomFormatInput` so existing `ICustomFormatSpecification.IsSatisfiedBy(CustomFormatInput input)` entry-point accepts it without sibling-overload bookkeeping. Manga specs cast `input is not MangaCustomFormatInput → return false`. Adds `ChapterInfo`, `Manga`, `Release`, `SourceKey` properties on top of base. Phase 8 collapse: with `CustomFormatInput`. |
| `src/NzbDrone.Core/Organizer/Manga/` (MangaFileNameBuilder + MangaPathBuilder + MangaNamingPresets) | new | Phase 5 | Per Phase 5 D-14 + D-16: parallel manga FileNameBuilder + reader-compat presets (Komga/Kavita/ComicRack/Custom). Sibling to `Organizer/FileNameBuilder.cs` + `Tv/SeriesPathBuilder.cs`. Pitfall 7 mitigation: `IFormattable.ToString(spec, CultureInfo.InvariantCulture)` for chapter-number padding (NOT custom regex) so `de-DE` cultures don't substitute `,` for `.`. Phase 8 collapse: canonical `Organizer/`. |
| `src/NzbDrone.Core/Organizer/Manga/MangaNamingPresets.cs` (in-code static list) | new | Phase 5 | Per Phase 5 D-16: Komga / Kavita / ComicRack / Custom — Phase 7 UI dropdown + first-run default = Komga. NO TV analog (Sonarr uses NamingConfig.Default factory only). DB-stored presets rejected (multi-row complexity; conflicts with NamingConfig-singleton invariant from Phase 1 D-04). Phase 8 cleanup: drop — manga becomes the only naming surface. |
| `001_mangarr_baseline.cs` schema deltas (TranslationProfiles + CustomFormatProfiles tables + Manga.TranslationProfileId/CustomFormatProfileId FK columns + NamingConfig.{StandardChapterFormat, MangaFolderFormat, RenameChapters} columns) | extend | Phase 5 | Per Phase 5 D-01 + D-07 + D-13 + dev-migration-policy.md edit-001-in-place rule (pre-v1). Reuses Sonarr's existing `StringListConverter<List<string>>` (TableMapping.cs:233) for `TranslationProfile.Languages` and existing `EmbeddedDocumentConverter<List<ProfileFormatItem>>` (TableMapping.cs:213) for `CustomFormatProfile.FormatItems` — NO new converter infrastructure (Adaptation Hotspots 8 + 9). Phase 8 cleanup: single coordinated baseline edit; sequential migrations resume post-v1.0.0 tag. |
| V5 controllers: `/api/v5/translationprofile` + `/api/v5/customformatprofile` + `/api/v5/customformat` (manga-spec-aware) + `/api/v5/config/manganaming` | new | Phase 5 | Per CLAUDE.md V5-is-current-API + Phase 5 plans 05-02..05-06: `TranslationProfileController`, `CustomFormatProfileController`, V5 `CustomFormatController` port (with `?mediaType=manga` filter per Open Question 3), `MangaNamingConfigController` (per Open Question 4 sibling). V3 controllers stay until Phase 8 collapse; new manga endpoints land directly in V5. Phase 8 cleanup: drop V3 controllers; all V5. |
| `src/NzbDrone.Core/Parser/Manga/Model/RemoteChapter.cs` Release widening (string → ReleaseInfo) + CustomFormats/CustomFormatScore additions | extend | Phase 5 | Wave 0 substrate fix (Rule 2): RemoteChapter previously typed Release as `string`; the decision pipeline (specs / comparer / maker) needs the full `ReleaseInfo` (TranslatedLanguage + IndexerPriority + Size + AgeMinutes + IndexerFlags + Title) so we widened to mirror `RemoteEpisode.Release` (Parser/Model/RemoteEpisode.cs:14). CF augmentation site adds `CustomFormats : List<CustomFormat>` + `CustomFormatScore : int` (mirrors `RemoteEpisode.CustomFormats`/`CustomFormatScore`). Phase 8 may rename / collapse with `RemoteEpisode`. |

## Phase 6 — Pipeline + Wanted/History/Blocklist + Reader Notify (2026-05-03)

Sibling-divergence entries per Phase 6 plan 06-01 (Wave 0 substrate). Every NEW manga-side
sibling file or schema delta gets a row; the parallel hierarchy collapses with `Tv/` deletion in Phase 8.

| File / Path | Type | Phase | Rationale |
|-------------|------|-------|-----------|
| `001_mangarr_baseline.cs` Phase 6 ChapterHistory table | extend | Phase 6 | Per Phase 6 D-21 + dev-migration-policy.md edit-001-in-place rule (pre-v1). Parallel sibling to `EpisodeHistory`: `ChapterId` column INDEPENDENT of `EpisodeHistory.EpisodeId` — closes Phase 5 BL-01 cross-domain ID-collision class of bug. Phase 8 cleanup: collapse with EpisodeHistory when Tv/ deletes. TableMapping registration deferred to Plan 06-03 (owns the ChapterHistory entity type). |
| `001_mangarr_baseline.cs` Phase 6 MangaBlocklist table | extend | Phase 6 | Per Phase 6 D-11 + dev-migration-policy.md edit-001-in-place rule. Release-level identity = `(SourceKey, ReleaseGuid, Title)` triple per D-11. Group-blocking expressible via existing -10000 ScanlationGroup CF (Phase 5 D-09) — no separate "block this group" UI in v1. Phase 8 collapse: with `Blocklist`. TableMapping registration deferred to Plan 06-04. |
| `001_mangarr_baseline.cs` Phase 6 ChapterFiles table | extend | Phase 6 | Per Phase 6 PIPELINE-04 + dev-migration-policy.md. Parallel sibling to `EpisodeFiles`. Phase 6 import artifact written by Plan 06-07 `ImportApprovedChapters`. Phase 8 collapse: with `EpisodeFile`. |
| `Manga.UpgradeAllowedOverride : bool?` column + entity property | extend | Phase 6 | Per Phase 6 D-10. Three-state semantics: NULL = fall back to per-Profile flag; TRUE = force-allow upgrades; FALSE = force-disallow. Mirrors the per-Manga FK + global default pattern Phase 5 D-01 + D-07 already established. Phase 8 cleanup: rename to canonical position when domain rename runs. |
| `Chapter.ChapterFileId : int?` column + entity property | extend | Phase 6 | Per Phase 6 PIPELINE-04. Manga sibling of `Episode.EpisodeFileId`. Plan 06-07 UpgradeSpec + Plan 06-09 Wanted query consume. Phase 8 cleanup: collapse with `Episode.EpisodeFileId`. |
| `TranslationProfile.UpgradeAllowed : bool` (default TRUE) | extend | Phase 6 | Per Phase 6 D-10 — language rank ordered, *arr promise. NO TV analog (Sonarr's `QualityProfile.UpgradeAllowed` is the `Quality`-axis equivalent; Phase 8 collapse may unify when Tv/ deletes). |
| `CustomFormatProfile.UpgradeAllowed : bool` (default FALSE) | extend | Phase 6 | Per Phase 6 D-10 — manga CF scores subjective; auto-churn risky. Default FALSE intentionally diverges from `TranslationProfile.UpgradeAllowed` default TRUE per D-10's orthogonal concerns. |
| `IndexerDefinition.SyncInterval : int` + `LastRssSync : DateTime?` columns + entity properties | extend | Phase 6 | Per Phase 6 D-07 — per-source override on top of global `Config.MangaRssSyncInterval`. SyncInterval default 0 = use global. LastRssSync mutated by `MangaRssSyncService` run; flagged `[MemberwiseEqualityIgnore]` to prevent comparison churn. |
| `IConfigService.MangaRssSyncInterval` (default 15min) + `MaxAutoRetriesPerChapter` (default 3) Config keys | extend | Phase 6 | Per Phase 6 D-07 (sync) + D-13 (bounded auto-retry safety floor). Lazy default-on-getter via `GetValueInt` matches Sonarr's `RssSyncInterval` pattern. |
| `src/NzbDrone.Core/MediaFiles/ChapterFile.cs` + `ChapterFileService` + `ChapterFileRepository` + `ChapterFileAddedEvent` + `ChapterFileDeletedEvent` | new | Phase 6 | Per Phase 6 PIPELINE-04. Parallel sibling to `MediaFiles/EpisodeFile.cs` + `MediaFileService.cs` + `MediaFileRepository.cs` + `EpisodeFileAddedEvent` + `EpisodeFileDeletedEvent`. `ChapterFileService` `IHandleAsync<MangaDeletedEvent>` cascade-deletes mirror `MediaFileService` `IHandleAsync<SeriesDeletedEvent>`. Plans 06-07/08/09 consume. Phase 8 collapse: with `EpisodeFile`. |
| `IChapterService.AllMissingMonitoredChapters()` + repository impl | extend | Phase 6 | Per Phase 6 D-09 — Wanted/Missing feed. Returns monitored chapters with `ChapterFileId IS NULL`. Manga.Monitored filter applied at consumer layer (Plan 06-06 MissingChapterSearchService). Sibling to TV's `EpisodeService.EpisodesWithoutFiles`. |
| `IChapterDownloadStateRepository.DeleteByChapterId(int)` | extend | Phase 6 | Per Phase 6 PIPELINE-04 lifecycle hook. Idempotent; consumed by Plans 06-07/08 after import success or terminal-fail+retention. Sibling to existing `DeleteOrphans` housekeeper sweep. |
| `src/NzbDrone.Core/MediaFiles/ChapterArchiving/ChapterGrabbedEvent.cs` | new | Phase 6 | Per Phase 6 D-21. Carries `RemoteChapter` + `DownloadId` + `DownloadClient`. Role-match analog: `Download/EpisodeGrabbedEvent.cs`. Emitted by Phase 4 InProcessImageDownloadClient after grab; consumed by Plan 06-03 `ChapterHistoryService.Handle`. Phase 8 collapse: with EpisodeGrabbedEvent. |
| `src/NzbDrone.Core/MediaFiles/ChapterArchiving/ChapterDownloadFailedEvent.cs` un-sealed + extended | extend | Phase 6 | Per Phase 6 D-19 + D-21. Un-sealed; gained optional `SourceTitle` / `Source` / `DownloadClient` / `Release : ReleaseInfo` init-only properties + `Reason`/`Message` aliases. Existing 4-arg ctor preserved so Phase 4 emit sites continue to compile (object-initializer syntax populates the new fields where data is in scope). Plans 06-03 history Data hydration + 06-04 blocklist auto-insert require. |
| `MangaPathBuilder.BuildChapterPath(manga, chapter, sourcePath)` + `IBuildMangaPaths` interface extension | extend | Phase 6 | Per Phase 6 PIPELINE-04. Composes `<manga.RootFolderPath>/<mangaFolder>/<chapterFileName>.<ext>` reusing Phase 5 `MangaFileNameBuilder.BuildFileName` + `GetMangaFolder`. Plan 06-07 `ImportApprovedChapters` consumes. Phase 8 collapse: canonical PathBuilder. |
| `INotification.OnChapterImport(ChapterImportMessage) + SupportsOnChapterImport` interface extension | extend | Phase 6 | Per Phase 6 D-18 + RESEARCH.md Pitfall 7. Cross-cutting interface change — without the interface hook the NotificationService fan-out for `ChapterImportedEvent` cannot dispatch even when Komga/Kavita providers (Plans 06-10/11) override the virtual base method. All existing TV providers compile because `NotificationBase` ships a virtual no-op default. Phase 8 cleanup: collapse with `OnImportComplete`. |
| `NotificationBase.OnChapterImport` virtual no-op + `SupportsOnChapterImport` reflection-backed flag | extend | Phase 6 | Per Phase 6 D-18. Reuses the existing `HasConcreteImplementation("OnChapterImport")` reflection helper (NotificationBase.cs:122) so providers that DO override return true and providers that DON'T (every TV notification) return false — safe by default. Phase 8 cleanup: collapse with the virtual `OnImportComplete` no-op when domain rename runs. |
| `NotificationService` `IHandle<ChapterImportedEvent>` fan-out + `OnChapterImportEnabled()` factory method | extend | Phase 6 | Per Phase 6 D-18 + Pitfall 7 (three-layer mitigation: interface + base virtual + service fan-out). `INotificationFactory.OnChapterImportEnabled()` filters `Definition.OnChapterImport && SupportsOnChapterImport` (NOT the legacy `GetAvailableProviders` shape that bypasses Definition-level user toggle). Per-provider try/catch matches existing event-handler pattern (T-06-03 mitigation). Phase 8 cleanup: collapse with `Handle(EpisodeImportedEvent)`. |
| `src/NzbDrone.Core/Notifications/ChapterImportMessage.cs` | new | Phase 6 | Per Phase 6 D-18 + Pitfall 7. POCO carrying `Manga + Chapter + ChapterFile + SourceTitle + SourcePath + DownloadClient + DownloadId + OldFiles`. Role-match analog: `Notifications/DownloadMessage.cs` (TV ImportComplete shape). Phase 8 cleanup: collapse with DownloadMessage when `Tv/` deletes. |
| `NotificationDefinition.OnChapterImport bool + SupportsOnChapterImport [MemberwiseEqualityIgnore]` | extend | Phase 6 | Per Phase 6 D-18. User toggle for Settings → Notifications. Default TRUE so newly-added Komga/Kavita providers fire `OnChapterImport` without an extra checkbox click; user can still disable per-provider. `Enable` composite predicate updated to include the new flag. Phase 8 cleanup: rename to canonical position when domain rename runs. |
| `src/NzbDrone.Core/MediaFiles/MangaImport/ChapterImportedEvent.cs` | new | Phase 6 | Per Phase 6 D-18. Carries `Manga + Chapter + ChapterFile + DownloadClientItem + NewDownload + SourcePath`. Role-match analog: `MediaFiles/Events/EpisodeImportedEvent.cs`. Pitfall 4 GUARD: published AFTER ChapterFile DB commit + filesystem move complete (Plan 06-07 `ImportApprovedChapters` enforces). Phase 8 cleanup: collapse with `EpisodeImportedEvent`. |
| `src/NzbDrone.Core/MediaFiles/MangaImport/ChapterImportFailedEvent.cs` | new | Phase 6 | Per Phase 6 D-12. Carries `Manga + Chapter + SourcePath + FailureReason + DownloadClientItem`. Covers IMPORT-stage failures (post-archive, pre-DB-commit) — distinct from Phase 4 `ChapterDownloadFailedEvent` (download-stage). Phase 8 cleanup: collapse with hypothetical `EpisodeImportFailedEvent` if/when Tv/ deletes. |

---
*Last updated: 2026-05-01 (Phase 0 — initial authoring)*
*Last updated: 2026-05-01 (Phase 0 — added TranslationProfile rows under Planned Divergences after cf-only-walkthrough verdict signoff: TranslationProfile added)*
*Last updated: 2026-05-01 (Phase 1 Plan 01 — appended 5 Phase 1 entries: HttpAggregatorBase, DownloadProtocol.Http, baseline migration replacement, Polly retry coverage extension, BusyTimeout 1000→5000)*
*Last updated: 2026-05-02 (Phase 3 Plan 06 — appended 6 Phase 3 entries: IIndexer manga overloads, HttpAggregatorBase manga abstracts + GetDownloadHeaders, MangaSearchCriteriaBase parallel hierarchy, IndexerSourceStatus per-SourceKey, ReleaseInfo ScanlationGroup + TranslatedLanguage, MangaDex + Comix indexers + MangaFire v2 descope)*
*Last updated: 2026-05-03 (Phase 5 plan 05-07 — appended 13 Phase 5 sibling-divergence entries: Profiles/Translations + Profiles/CustomFormats, DecisionEngine/Manga + comparer + 11 specs, 4 manga CF specs + MediaType discriminator + MangaCustomFormatInput, Organizer/Manga + MangaNamingPresets, baseline schema deltas, V5 controllers, RemoteChapter widening)*
*Last updated: 2026-05-03 (Phase 6 plan 06-01 — appended 14 Phase 6 entries: ChapterHistory + MangaBlocklist + ChapterFiles tables, UpgradeAllowed columns, Manga.UpgradeAllowedOverride, Chapter.ChapterFileId, IndexerDefinition SyncInterval+LastRssSync, MangaRssSyncInterval/MaxAutoRetriesPerChapter Config keys, ChapterFile substrate, AllMissingMonitoredChapters, DeleteByChapterId, ChapterGrabbedEvent, ChapterDownloadFailedEvent extension, BuildChapterPath)*
*Last updated: 2026-05-03 (Phase 6 plan 06-02 — appended 7 Phase 6 entries: INotification.OnChapterImport hook + Supports flag, NotificationBase virtual + reflection-backed Supports, NotificationService.IHandle<ChapterImportedEvent> + OnChapterImportEnabled() factory, ChapterImportMessage POCO, NotificationDefinition.OnChapterImport flag, ChapterImportedEvent + ChapterImportFailedEvent under MediaFiles/MangaImport/)*
